# node_attribute_type_mismatch

Purpose: Invalid fixture for NODE_ATTRIBUTE_TYPE_MISMATCH.

Expected validity: invalid

Covers:
- existing Slice 1B validation contract
- deterministic validation output

Limitations:
- no transformer input dependency
- no persistence, selection, projection, or Reader behavior

Expected primary validation code: NODE_ATTRIBUTE_TYPE_MISMATCH
