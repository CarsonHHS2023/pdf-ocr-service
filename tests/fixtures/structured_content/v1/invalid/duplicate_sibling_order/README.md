# duplicate_sibling_order

Purpose: Existing invalid fixture preserved for DUPLICATE_SIBLING_ORDER.

Expected validity: invalid

Covers:
- existing invalid fixture semantics
- deterministic validation output

Limitations:
- no transformer input dependency
- no persistence, selection, projection, or Reader behavior

Expected primary validation code: DUPLICATE_SIBLING_ORDER
