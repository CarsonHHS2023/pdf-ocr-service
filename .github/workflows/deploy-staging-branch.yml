name: DEPRECATED - standalone staging branch deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Reject deprecated deployment path
        run: |
          echo "::error::DEPRECATED: Staging deployment is owned by the deploy job in Staging Backend Integration CI after all integration contracts pass."
          exit 1
